import './TextArea.css';

export default function TextArea() {
  
  return (
      <textarea
        className='textArea'
        placeholder = "Опишите чем придется заниматься на данной должности"
      />
    );
}
